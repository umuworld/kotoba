# 安全与隐私

这是早期学习工具，没有经过独立安全审计。优先使用当前 main 分支；不承诺旧版本安全回补时限。

## 报告问题

不要在公开 Issue 或 PR 中上传真实 API Key、语音、私人字幕、系统完整日志或个人配置。若仓库 Security 页提供私密漏洞报告入口，请使用该入口；否则先创建不含漏洞利用细节的 Issue 请求私密沟通渠道。

若密钥已泄漏，先在服务商后台撤销或轮换密钥，不要仅删除帖子。

## 当前保护措施

- 密钥保存在 macOS 钥匙串，按完整接口端点隔离。
- 远程 API 地址要求 HTTPS，HTTP 仅允许本机回环地址。
- AI 请求拒绝自动跟随重定向，避免携带凭据跳转。
- 音频和视频不写入磁盘；本地字幕与收藏未额外加密，请依赖系统账户、磁盘加密及设备安全措施保护。
- 默认测试只访问本机模拟接口。发布仓库不包含已签名/已公证的通用安装程序。

## 使用提醒

AI 模型输出与导入字幕属于不可信内容。不要把其中的命令或建议当作系统指令。请自行检查自定义 API 服务的可信性和数据保留政策。
