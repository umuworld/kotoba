# 参与贡献

感谢你帮助改进 Kotoba。请先阅读 README 的环境、隐私和限制说明。

## 开发流程

1. Fork 仓库并新建工作分支。
2. 安装 macOS SDK 26+ 的工具链，在 Apple Silicon Mac 上运行 `bash build.sh`。
3. 修改代码后运行 `bash Tests/run.sh`；窗口相关改动还需在本地运行 `dist/Kotoba.app/Contents/MacOS/Kotoba --self-test` 并手动检查。
4. 提交 Pull Request，说明动机、改动范围、验证结果和已知限制。

无需第三方 Swift 包。请优先使用公开的 Apple API，不依赖私有接口、权限绕过或网站账号 Cookie。

## 提交前检查

- 不提交 `.build/`、`dist/`、应用包、签名私钥、证书、API Key 或本地设置。
- 测试数据使用占位凭据，不让默认测试请求真实云端 AI。
- 界面截图只使用自有或获授权的内容，遮盖账户信息与私人字幕。
- 新增 AI 协议逻辑时扩展本机 HTTP 合约测试，确认错误处理、上下文及认证头隔离。
- 窗口改动检查激活/失活、输入框焦点、缩放、深浅外观与减少透明度。
- 不承诺尚未测试的平台或模型支持。

贡献提交按本仓库 MIT 许可证提供。请确保你有权贡献提交的代码与素材。
